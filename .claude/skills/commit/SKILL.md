# Commit Workflow
1. Run the full test suite: `python -m pytest`
2. Run `black .` to format
3. Show me the diff summary and test count
4. Ask which branch to push to
5. Commit with a conventional commit message and push
